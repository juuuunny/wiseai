upstream backend {
  server backend-prod-blue:8080;
}

# ★ Kibana 업스트림 추가
upstream kibana_prod {
  server kibana-prod:5601;   # NGINX가 같은 docker 네트워크에 있어야 함
  # NGINX가 호스트에서 돌면: server 127.0.0.1:5601;
}

# ★ Grafana 업스트림 추가
upstream grafana_prod {
  server grafana-prod:3000;   # NGINX가 같은 docker 네트워크에 있어야 함
  # NGINX가 호스트에서 돌면: server 127.0.0.1:3000;
}


# HTTP to HTTPS redirect for api.dataracy.store (운영 환경은 HTTPS 강제)
server {
  listen 80;
  server_name api.dataracy.store;
  return 301 https://$server_name$request_uri;
}


# HTTPS server for api.dataracy.store (운영 환경)
server {
  listen 443 ssl;
  http2 on;
  server_name api.dataracy.store;

  ssl_certificate     /etc/nginx/ssl/cloudflare/origin.crt;
  ssl_certificate_key /etc/nginx/ssl/cloudflare/origin.key;

  # TLS 보안 옵션
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers on;

  client_max_body_size 50m;
  client_body_timeout 120s;

  # 공통 헤더
  proxy_set_header Host $host;
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Proto $scheme;
  proxy_http_version 1.1;
  proxy_set_header Upgrade $http_upgrade;
  proxy_set_header Connection "upgrade";
  proxy_set_header Cookie $http_cookie;

  # API 요청 프록시
  location /api {
    proxy_pass http://backend;
    proxy_request_buffering off;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;
  }

  # Swagger UI (정적 리소스) - 캐시 무효화
  location /swagger-ui/ {
    proxy_pass http://backend/swagger-ui/;
    add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
    add_header Pragma "no-cache" always;
    expires -1;
    etag off;
  }

  # OpenAPI JSON - 캐시 무효화
  location /v3/api-docs {
    proxy_pass http://backend/v3/api-docs;
    add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
    add_header Pragma "no-cache" always;
    expires -1;
    etag off;
  }

  # Swagger UI가 참조하는 설정 - 캐시 무효화
  location /swagger-config {
    proxy_pass http://backend/swagger-config;
    add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
    add_header Pragma "no-cache" always;
    expires -1;
    etag off;
  }

  # (선택) webjars도 최신 강제 반영 원하면 캐시 무효화
  location /webjars/ {
    proxy_pass http://backend/webjars/;
    add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
    add_header Pragma "no-cache" always;
    expires -1;
    etag off;
  }

  # Kibana 프록시 (경로 /kibana)
  location /kibana/ {
    proxy_pass http://kibana_prod/kibana/;     # 뒤 슬래시 필수
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;

    # WebSocket
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    # 프록시 뒤 basePath 사용 시 권장 헤더
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /kibana;
  }

  # Grafana 프록시 (경로 /grafana)
  location /grafana/ {
    proxy_pass http://grafana_prod/;     # 뒤 슬래시 필수
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;

    # WebSocket
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    # 프록시 뒤 basePath 사용 시 권장 헤더
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /grafana;
  }

  # 헬스 체크
  location /actuator/health {
    proxy_pass http://backend/actuator/health;
  }

  # 나머지 fallback
  location / {
    proxy_pass http://backend;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Cookie $http_cookie;
  }
}
